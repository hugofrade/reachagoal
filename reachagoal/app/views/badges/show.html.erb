<p id="notice"><%= notice %></p>

<p>
  <strong>Statement:</strong>
  <%= @badge.statement %>
  <%= image_tag @badge.icon.url(:icon) %>
</p>

<%= link_to 'Edit', edit_badge_path(@badge) %> |
<%= link_to 'Back', badges_path %>
